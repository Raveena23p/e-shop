import React from 'react';
import IconButton from '@material-ui/core/IconButton';
import Signin from '../Sign in';
import LockOutlinedIcon  from '@mui/icons-material/LockOutlined';

const MyComponent = () => {
  return (
    <IconButton className="LockOutlinedIcon">
      <YourIcon />
    </IconButton>
  );
};

